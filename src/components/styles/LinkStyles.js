import { makeStyles } from "@material-ui/core/styles";

export const useLinkStyles = makeStyles(() => ({
  navlink: {
    textDecoration: "none",
    color: "white"
  },
  navbarLink: {
    textDecoration: "none",
    color: "black"
  }
}));
